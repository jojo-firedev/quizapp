import 'package:flutter/material.dart';

class PointInputScreen extends StatelessWidget {
  const PointInputScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
      child: Text('Punkte eingabe'),
    ));
  }
}
